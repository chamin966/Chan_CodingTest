이진탐색을 이용하면 정렬된 배열에서 특정 원소의 개수 또는
값이 특정 범위에 해당하는 원소의 개수를 O(logN) 시간 안에 구할 수 있다.

1. 하한선 함수와 상한선 함수를 사용하여 왼쪽값과 오른쪽 값 구하기
: 하한선 함수와 상한선 함수 사용
하한선 함수 lowerBound(arr, x)
: 정렬된 순서를 유지하면서 배열 arr에 x가 들어갈 수 있는 가장 왼쪽 인덱스를 반환
상한선 함수 upperBound(arr, x)
: 정렬된 순서를 유지하면서 배열 arr에 x가 들어갈 수 있는 가장 오른쪽 인덱스를 반환
2. 상한선 함수 반환값 - 하한선 함수 반환값